{
  "name": "echo-demo",
  "version": "1.0.0",
  "description": "ECHO Protocol Demo on Qitmeer Network",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:ipfs": "node test/test-ipfs.js",
    "deploy:testnet": "hardhat run scripts/deploy.js --network qitmeerTestnet",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^4.9.0",
    "dotenv": "^17.3.1",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "form-data": "^4.0.5",
    "web3": "^4.16.0"
  }
}
